{
  "name": "cowrie-gateway",
  "version": "1.0.0",
  "description": "Cowrie — a fully functional demo payment gateway (Node/Express). Links the landing, login and checkout pages to a real backend.",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
